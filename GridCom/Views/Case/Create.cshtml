@model GridCom.Models.CasesOfOutage

@{
    ViewData["Title"] = "Создание";
}

<h1>Создать новую запись</h1>

<h4>Причина отключения</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Case_outage" class="control-label">Причина</label>
                <input asp-for="Case_outage" class="form-control" />
                <span asp-validation-for="Case_outage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Создать" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
